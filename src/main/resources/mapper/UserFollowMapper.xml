<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nuo.nuopaoserver.mapper.UserFollowMapper">
    <select id="followList" resultType="com.nuo.nuopaoserver.vo.FollowPageVo">
        SELECT
            u.id,
            u.username,
            u.avatarUrl,
            u.planetCode,
            f.createTime AS followTime
        FROM user_follow f
                 JOIN `user` u ON u.id = f.followUserId
        WHERE f.userId = #{dto.userId}
        ORDER BY f.createTime DESC
    </select>
</mapper>