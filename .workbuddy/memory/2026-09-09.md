
## 代码评审与 bug 修复（21:05）
- 对 nuopaoserver 做了整体代码评审（分层结构、安全、设计三层）。
- 修复 3 个功能性 bug：
  1. MvcConfig excludePathPatterns 补充 "/user/login"（原 `/user/login/*` 不匹配密码登录接口本身，导致被登录拦截器挡住）。
  2. RefreshTokenInterceptor 续期判断改为带前缀 key `user_login_token:xxx`（原裸 token 判断永不命中，续期从未生效）。
  3. UserServiceImpl.emailLogin 查库后补判空（用户被逻辑删除时避免 NPE）。
- 注意：项目 mvnw wrapper 已损坏（缺 classworlds launcher），无法命令行编译验证，改动靠人工复查确认。
- 遗留未修（用户说后面再说）：验证码防暴力穷举、用户枚举提示、日志打印验证码明文、全局异常不打日志/泄露 e.getMessage()、@Async 发信失败验证码 key 残留、register 手写 Snowflake 冗余、TagController 越层+userId 为 null、401 响应无 charset、Result 未泛型化等。

## 前端页面重写（21:14）
- 按用户要求删除 nuoPao-front 旧页面（login.vue、stores/counter.js）并重写。
- 新增：src/utils/request.js（axios 封装：token header、统一 Result 处理、401 跳登录）、src/api/user.js + tag.js、src/stores/user.js（pinia + localStorage 持久化）、views/login.vue（双 tab：密码登录/邮箱验证码登录，60s 倒计时发码）、views/register.vue、views/home.vue（用户卡片+创建标签+退出登录）。
- router/index.js 重写：/ → /home redirect，meta.requiresAuth 守卫，已登录访问 login/register 跳 /home。
- 修正 vite.config.js 代理目标：localhost:3000 → localhost:8080（后端默认端口）。
- 前后端契约：后端读 header 名为 "token"；登录响应 LoginVo {token, username, ...}；登录接口字段 username/userPassword，注册 email/username/userPassword/code。
- `npm run build` 验证通过；5173 端口已有用户自启的 Vite 服务在跑，热加载生效。注意：WorkBuddy 后台任务的 cwd 固定在 session 目录，无法 cd 启动前端 dev。

## 标签接口测试与重复校验（22:33）
- 用前端账号 nuo/rpkbn592 登录拿 token，造了 8 条标签数据（运动/技术/生活 + 篮球/跑步/Java/Vue/美食），GET /tag 树查询正常。
- 验证：同名校验原先确实缺失（"运动"成功插入了第二条）。
- TagService 新增 saveTag：selectCount 同名校验抛 BizException；userId 从 UserContext 取（修复原先 save 后 userId 为 null 的问题）；TagController.addTag 改为调 service（下沉分层）。
- 用户自己已把建树改成两遍法；我在其基础上补了孤儿节点 parent==null 防御、list 按 id 排序、私有化 createTagTree、清死代码。
- 待办：改后代码需重启后端生效；重启后重测同名插入应返回 code 500 "标签[xx]已存在"。数据里有一条重复的"运动"（两条同名一级标签），可留作测试或手动删。

## 标签层级限制实现（22:36）
- 用户在 application.yml/application-local.yml 配了 tag.max-level: 5 但无代码读取；且 application.yml 中 max-level: ${tag.max-level} 是自引用占位符（靠 local 覆盖才没报循环引用），已改为字面量 5。
- 采用用户提议的 level 冗余字段方案：tag 表加 level int NOT NULL DEFAULT 1（AFTER parentId），存量数据回填（顶级=1，二级=2，已验证）。
- Tag 实体、TagVo 加 level；saveTag 创建时 level = 父.level + 1，父不存在报错，超 maxLevel 报错（@Value("${tag.max-level}")）；同时补了父标签存在性校验（原先 parentId 随便填都能成功，产生孤儿数据）。
- init.sql 同步加列。待办：重启后端后重测层级校验（造 5 级链 + 试第 6 级应报错）。
